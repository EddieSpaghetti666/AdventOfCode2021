#include <vector>
#include <string>
//int depthIncreases(const std::string& fileName);
int depthIncreases(const std::vector<int>& inputData);
int windowDepthIncreases(const std::vector<int>& inputData);
